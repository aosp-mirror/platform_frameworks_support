package androidx.security.content;

import android.content.Context;

// TODO Build this out, to allow for encrypting data to store in SharedPreferences
public class SharedPreferencesCompat {

    private Context mContext;

    public SharedPreferencesCompat(Context context) {
        mContext = context;
    }

}
